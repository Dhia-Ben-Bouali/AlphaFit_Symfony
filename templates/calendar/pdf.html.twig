<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Ma To-Do List</title>
    <style>
        body {
            font-family: 'Helvetica', sans-serif;
            margin: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            background-color: #fff;
            font-family: 'YourCustomFont', sans-serif;

        }

        .center-table {
            margin-top: 20px;
        }

        h1 {
            color: #3498db;
            text-align: center;
        }

        p {
            font-size: 12px;
            color: #777;
            text-align: center;
        }

        table {
            border-collapse: collapse;
            width: 80%;
            max-width: 800px;
            margin: 0 auto;
        }

        th, td {
            padding: 12px;
            text-align: left;
        }

        th {
            background-color: #3498db;
            color: #fff;
        }

        tr:nth-child(even) {
            background-color: #ecf0f3;
        }

        tr:hover {
            background-color: #d5e8f1;
        }

        .task-title {
            font-size: 16px;
            font-weight: bold;
        }

        .task-description {
            font-size: 14px;
            color: #555;
        }

        .task-date {
            font-size: 12px;
            color: #888;
        }

        .no-border tr {
            border: none;
        }

        .no-border td {
            border: none;
        }
         @font-face {
        font-family: 'Indie Flower';
        font-style: normal;
        font-weight: 400;
        src: url(data:font/truetype;base64,AAEAAAALAIAAAwAwT1MvMgEhO6GAAE4AAAAVE9TLzLkuKkCAAAIwAAAAMAAAACA...);
    }
    body {
        font-family: 'Indie Flower';
    }
    </style>
</head>
<body>
 Généré le {{ "now"|date("Y-m-d H:i:s") }}
   <h1 style="font-family: 'Indie Flower';">To Do list For this week</h1>


    <table class="no-border">
        <tbody>
            <tr style="background-color: #ecf0f3;">
                <td> Subscriber First Name &nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;: {{app.user.prenom }}</td>
            </tr>
            <tr style="background-color: #ecf0f3;">
                <td> Subscriber Last Name &nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;: {{app.user.prenom }}</td>
            </tr>
            <tr style="background-color: #ecf0f3;">
                <td> To DO list pour la Semaine&nbsp;&nbsp;: {{ startOfWeek.format('Y-m-d') }} à {{ endOfWeek.format('Y-m-d') }}</td>
            </tr>
        </tbody>
    </table>
    <br><br>
    <table class="center-table">
        <thead>
            <tr> 
                <th>Done</th>
                <th>Not Done</th>
                <th>Day </th>
                <th>To Do</th>
                <th>Description</th>
                <th>Start Time</th>
                <th>End Time</th>
            </tr>
        </thead>
        <tbody>
            {% for todo in data %}
                <tr>
                    <td><input type="checkbox"></td>
                    <td><input type="checkbox"></td>
                    <td class="task-title">{{ todo.start.format('l') }}</td>
                    <td class="task-description">{{todo.title}}</td>
                    <td class="task-date">{{todo.description}}</td>
                    <td class="task-date">{{ todo.start.format('H:i:s') }}</td>
                    <td class="task-date">{{ todo.end.format('H:i:s') }}</td>
                </tr>
            {% endfor %}
        </tbody>
    </table>
    <br><br><br><br>
       

</body>
</html>
