
{% extends 'base.html.twig' %}

{% block content %}
    <html lang="en">
    <!--begin::Head-->
    <head>

    <style>
    table {
        width: 90%; /* Adjust the width of the table */
        margin-left: auto; /* Set left margin to auto for center alignment */
        margin-right: auto; /* Set right margin to auto for center alignment */
        border-collapse: collapse;
        border-radius: 8px; /* Adding border radius to the table */
        overflow: hidden; /* Ensuring content does not overflow rounded corners */
    }
    th, td {
        padding: 12px;
        text-align: left;
        border-bottom: 1px solid #dddddd;
    }
    th {
        background-color: #f2f2f2;
        font-weight: bold;
    }
    tr:nth-child(even) {
        background-color: #f9f9f9; /* Light gray background for even rows */
    }
    tr:nth-child(odd) {
        background-color: #ffffff; /* White background for odd rows */
    }
    tr:hover {
        background-color: #e9e9e9; /* Hover effect for rows */
    }
    </style>

    </head>
    <!--end::Head-->
    <!--begin::Body-->
    <body id="kt_body" class="app-blank bgi-size-cover bgi-position-center">
    <!--begin::Theme mode setup on page load-->
    <script>var defaultThemeMode = "light"; var themeMode; if ( document.documentElement ) { if ( document.documentElement.hasAttribute("data-bs-theme-mode")) { themeMode = document.documentElement.getAttribute("data-bs-theme-mode"); } else { if ( localStorage.getItem("data-bs-theme") !== null ) { themeMode = localStorage.getItem("data-bs-theme"); } else { themeMode = defaultThemeMode; } } if (themeMode === "system") { themeMode = window.matchMedia("(prefers-color-scheme: dark)").matches ? "dark" : "light"; } document.documentElement.setAttribute("data-bs-theme", themeMode); }</script>
    <!--end::Theme mode setup on page load-->
    <!--begin::Root-->
    <div class="d-flex flex-column flex-root" id="kt_app_root">
        <!--begin::Page bg image-->
        <style>body { background-image: url('public/media/auth/bg9-dark.jpg'); } [data-bs-theme="dark"] body { background-image: url('/public/media/auth/bg9-dark.jpg'); }</style>
        <!--end::Page bg image-->
        <!--begin::Authentication - Sign-up -->
        <div class="">
            <h1 class="text-white fs-2qx fw-bold text-center mb-7">Welcome To Your Dashboard </h1>
                <div>
                    <table>
                        <thead>
                            <tr>
                                <th>User ID</th>
                                <th>Pack</th>
                                <th>Client</th>
                                <th>Schedule</th>
                                <th>Message</th>
                            </tr>
                        </thead>
                        <tbody>
                            {% for abonnement in abonnements %}
                                <tr>
                                    <td>{{ abonnement.client.id }}</td>
                                    <td>{{ abonnement.npack.nom }}</td>
                                    <td>{{ abonnement.client.username }}</td>
                                    <td>
                                        <a href="{{ path('index', {'id': abonnement.client.id }) }}">
                                            <img src="{{ asset('images/calendrier.png') }}" alt="Schedule Icon">
                                        </a>
                                    </td>
                                    <td>
                                        <a href="{{ path('app_messagerie') }}">
                                            <img src="{{ asset('images/messager.png')}}" alt="Schedule Icon">
                                        </a>
                                    </td>

                                </tr>
                            {% endfor %}
                        </tbody>
                    </table>
            </div>
        <!--end::Authentication - Sign-up-->
    </div>
    <!--end::Root-->
    <!--begin::Javascript-->
    <script src="{{ asset('plugins/global/plugins.bundle.js')}}"></script>
    <script src="{{ asset('js/scripts.bundle.js')}}"></script>
    <!--end::Global Javascript Bundle-->
    <!--end::Custom Javascript-->
    <!--end::Javascript-->
    </body>
    <!--end::Body-->
    </html>

{% endblock %}





                 
